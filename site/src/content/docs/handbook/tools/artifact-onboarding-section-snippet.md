---
title: ollama_artifact_onboarding_section_snippet
description: "Renders a handbook-ready `## What this repo is` section from a repo_pack artifact — thesis, key surfaces, read-next, runtime hints."
---

<!-- GENERATED by scripts/gen-tool-docs.mjs — do not edit by hand. Source of truth: the tool's zod schema (.describe() strings) + its registration in src/index.ts. Regenerate: npm run gen:tool-docs -->

**Onboarding-section snippet.** ARTIFACT. Renders a handbook-ready `## What this repo is` section from a repo_pack artifact — thesis, key surfaces, read-next, runtime hints. Investigative tone preserved (read-next is LOOK AT, not prescriptive). No model call. Returns `{rendered, metadata}`.

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `slug` | string | yes | — | Slug of the source artifact (from artifact_list). |
| `extra_artifact_dirs` | string[] | no | — | Extra read-only search dirs (same as artifact_read). |

## Full input schema

The JSON Schema below is generated from the same zod schema the server validates against at the wire — it cannot drift from runtime behavior.

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "slug": {
      "type": "string",
      "minLength": 1,
      "description": "Slug of the source artifact (from artifact_list)."
    },
    "extra_artifact_dirs": {
      "description": "Extra read-only search dirs (same as artifact_read).",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "required": [
    "slug"
  ]
}
```

## Envelope

Every tool returns the standard envelope — `tier_used`, `model`, `tokens_in`/`tokens_out`, `elapsed_ms`, backend provenance, warnings. See [Envelope & tiers](../envelope-and-tiers/).

## Source

Schema + handler: [`src/tools/artifactSnippets.ts`](https://github.com/mcp-tool-shop-org/ollama-intern-mcp/blob/main/src/tools/artifactSnippets.ts) · registration: [`src/index.ts`](https://github.com/mcp-tool-shop-org/ollama-intern-mcp/blob/main/src/index.ts)

---

_Back to the [Tool Reference](../tools/)._
